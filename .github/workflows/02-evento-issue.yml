name: 02-evento-issue

on:
  issues:
    types: [opened]

jobs:
  evento-issue:
    runs-on: ubuntu-latest

    steps:
      - name: Hola Mundo
        run: echo "Hola Mundo, el usuario creó un nuevo issue"