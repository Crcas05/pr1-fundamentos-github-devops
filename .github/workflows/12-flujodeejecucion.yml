name: 12-flujodeejecucion

on:
  workflow_dispatch:

jobs:

  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Ejecutando Job 1
        run: echo "Este es el Job 1"

  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: Ejecutando Job 2
        run: echo "Este es el Job 2"

  job3:
    runs-on: ubuntu-latest
    needs: job2
    steps:
      - name: Ejecutando Job 3
        run: echo "Este es el Job 3"